import { StyleSheet } from 'react-native';
import { Typography } from '@/constants/Typography';

export const styles = StyleSheet.create({
  filterPanel: {
    borderBottomLeftRadius: 28,
    borderBottomRightRadius: 28,
    backgroundColor: '#FFF9F1',
    borderWidth: 1,
    borderColor: '#E8DFC7',
    padding: 16,
  },
  filterOverlay: {
    flex: 1,
    justifyContent: 'flex-start',
  },
  filterBackdrop: {
    ...StyleSheet.absoluteFillObject,
    backgroundColor: 'rgba(44,52,32,0.26)',
  },
  filterSheet: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    overflow: 'hidden',
  },
  filterHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 8,
  },
  categoryBackBtn: {
    width: 30,
    height: 30,
    borderRadius: 16,
    alignItems: 'center',
    justifyContent: 'center',
  },
  filterMainTitle: {
    fontSize: Typography.size.bodyLg,
    fontWeight: '800',
    color: '#2C3420',
  },
  filterHeaderHint: {
    marginTop: 2,
    fontSize: Typography.size.caption,
    color: '#7F8671',
  },
  filterCardBlock: {
    borderRadius: 18,
    backgroundColor: '#F6EDE4',
    borderWidth: 1,
    borderColor: '#E8DFC7',
    padding: 12,
  },
  filterTitle: { fontSize: Typography.size.label, color: '#2C3420', fontWeight: '700' },
  filterBlockGap: { marginTop: 14 },
  rangeRow: {
    marginTop: 10,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 10,
  },
  rangeInput: {
    flex: 1,
    height: 44,
    borderRadius: 14,
    backgroundColor: '#FFF9F1',
    borderWidth: 1,
    borderColor: '#E8DFC7',
    paddingHorizontal: 12,
    color: '#2C3420',
    fontSize: Typography.size.body,
  },
  rangeDivider: { color: '#7F8671', fontSize: Typography.size.caption },
  helperText: { marginTop: 8, color: '#7F8671', fontSize: Typography.size.caption },

  typeWrap: {
    marginTop: 10,
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: 8,
  },
  typeChip: {
    minWidth: 70,
    height: 40,
    borderRadius: 14,
    backgroundColor: '#FFF9F1',
    borderWidth: 1,
    borderColor: '#E8DFC7',
    alignItems: 'center',
    justifyContent: 'center',
    paddingHorizontal: 10,
  },
  typeChipActive: {
    backgroundColor: '#FCCEB4',
    borderColor: '#F98C58',
  },
  typeChipText: { color: '#7F8671', fontSize: Typography.size.body, fontWeight: '700' },
  typeChipTextActive: { color: '#DD724A' },

  filterRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    borderRadius: 18,
    backgroundColor: '#F6EDE4',
    borderWidth: 1,
    borderColor: '#E8DFC7',
    paddingHorizontal: 12,
    minHeight: 52,
  },
  filterRowRight: { flexDirection: 'row', alignItems: 'center' },
  filterValue: { color: '#7F8671', fontSize: Typography.size.body },

  filterActions: {
    marginTop: 14,
    flexDirection: 'row',
    gap: 10,
  },
  resetBtn: {
    flex: 1,
    height: 44,
    borderRadius: 14,
    backgroundColor: '#EEF3D8',
    alignItems: 'center',
    justifyContent: 'center',
  },
  resetText: { color: '#6E7D42', fontSize: Typography.size.label, fontWeight: '700' },
  confirmBtn: {
    flex: 1,
    height: 44,
    borderRadius: 14,
    backgroundColor: '#F98C58',
    alignItems: 'center',
    justifyContent: 'center',
  },
  confirmText: { color: '#FFFFFF', fontSize: Typography.size.label, fontWeight: '700' },

  categoryList: {
    maxHeight: 340,
    marginTop: 8,
  },
  categoryItem: {
    minHeight: 48,
    justifyContent: 'center',
    paddingHorizontal: 12,
    borderRadius: 14,
    backgroundColor: '#F6EDE4',
    borderWidth: 1,
    borderColor: '#E8DFC7',
    marginBottom: 8,
  },
  categoryItemText: { fontSize: Typography.size.body, color: '#2C3420', fontWeight: '600' },
});
